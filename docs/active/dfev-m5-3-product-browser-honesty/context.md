# DFEV M5-3 context

- Exact source baseline: `65379234ff1c856ef121505d69b98d0758baf312` (`codex/dfev1-m1-m6-supervisor`).
- M5-1 is a fixed public City Hall to Independence Hall loopback probe and replayable local batch only. It grants neither arbitrary candidate generation nor private product runtime promotion.
- M5-2 removed the production verifier injection boundary. Test options, adapters, environment declarations, and fixtures cannot grant authority.
- The browser bundle ceiling remains 4,000,000 bytes with only 80 bytes of source-baseline headroom.
- M5-3 therefore owns test and acceptance files only. It does not change product code, M1-M4 evidence, retained `.dfev1` roots, deployment, or remote refs.
