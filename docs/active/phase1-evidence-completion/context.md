# Phase 1 Evidence Completion Context

## Baseline

- 2026-08-21: clean detached worktree; `HEAD`, local `main`, and merge-base are
  `91cba5544f6a1ae7dc8c26a9d265657f452aae3b`.
- Required DFEV closeout SHA is an ancestor of both `HEAD` and local `main`.
- This task owns no listener before verification begins. Other worktrees are
  preserved and are not mutated by this task.

## Live-process contract

| Command class | Owner | Cwd | Shared ports/output | Log/result policy | Stop condition |
| --- | --- | --- | --- | --- | --- |
| Focused Node contracts | Phase 1-0 task | this worktree | none | terminal exit/output recorded in `task.md` | command exits |
| Build / bundle policy | Phase 1-0 task | this worktree | `dist/` only | serial; `dist/` is regenerated by Vite | command exits |
| Area Intelligence browser | Phase 1-0 task | this worktree | port 4192; `test-results/area-intelligence-browser/` | run alone; suite closes preview/browser | suite cleanup completes |
| Home Compare browser | Phase 1-0 task | this worktree | port 4193; `test-results/home-compare-browser/` | run alone; suite closes preview/browser | suite cleanup completes |
| Known Route browser | Phase 1-0 task | this worktree | port 4194; `test-results/known-route-evidence-browser/` | run alone; suite closes preview/browser | suite cleanup completes |
| Existing release visual suite | Phase 1-0 task | this worktree | Playwright-managed server/output | run only after the three explicit suites | suite cleanup completes |

No command in this task starts a persistent service. Repeated identical failure
under the same hypothesis stops after the third occurrence and is diagnosed
instead of rerun.

## Evidence and decisions

- The archived DFEV M0 record establishes recoverable serial replacement with
  rollback. Its historic phrase "atomic" is retained only where it names a
  function/test; active human-facing wording must use the precise transaction
  description in `plan.md`.
- Existing Source Health contracts already preserve four immutable clocks and
  status separation. Phase 1-0 adds only mechanical coverage that prevents
  release wiring and shared lifecycle semantics from drifting.
- Initial `npm run build:manifest` stopped before Vite because `node_modules`
  was absent. `npm ci` then restored exactly 395 lockfile packages (396 audited,
  0 vulnerabilities) without changing package metadata; this was a local test
  prerequisite, not a dependency change.
- The release runner now owns Area Intelligence, Home Compare, and Known Route
  browser suites exactly once. The workflow contract asserts their package
  scripts, required Chromium install, the single CI release invocation, and the
  complete release step order.
- Home Compare results rendering is now a query-time lazy chunk. The existing
  18,000 gzip-byte controller ceiling remains unchanged; the local bundle gate
  measured controller `30,427/10,799` raw/gzip bytes, leaving 7,201 gzip bytes
  of headroom, and results `3,672/1,504` raw/gzip bytes.

## Handoff checkpoint

Phase 1-0 is ready for integration review. Integration, remote CI, scheduled
workflow execution, deployment, and live-source verification remain unrun and
unauthorized here.
