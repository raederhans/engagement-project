# S3-C2 task status

## Current state

Integrated and archived. The scheme-B data/privacy admission remains commit
`6f0c2c2`, preserved as an ancestor of merge commit `b09d4a2` and synchronized
through `main`. Combined-tree validation and browser/visual checks passed after
the shared lazy-import and bundle contracts were manually composed.

## Integration completion

- [x] Preserved the exact C2 SHA and its dependency on C1.
- [x] Combined task-focus, query-preset, incident-result, chart, catalog, and
  route-corridor lazy manifest assertions without alternate state ownership.
- [x] Remeasured the combined and CI-equivalent builds; main Entry decreased,
  while the Crime lazy ceiling received a total 750 raw / 250 gzip adjustment.
  The final 250 raw bytes cover the feature-enabled CI/Pages build only.
- [x] Proved `origin/main` contains `6f0c2c2`, then unregistered the isolated
  Git worktree and deleted `codex/s3-c2-route-corridor-data` locally.
- [x] Retained the explicit gaps: no GPS matching, final UI, real-time or future
  risk claim, and no safest-route recommendation.

## Checklist

- [x] Preserve primary-worktree WIP and identify the sole red overlap.
- [x] Commit reviewed S3-C1 foundation (`751b5ef`; now 12/12 focused tests).
- [x] Verify source schema and choose a single-statement snapshot response.
- [x] Verify official CARTO/PostgreSQL/PostGIS/GeoJSON/File API contracts.
- [x] Observe RED for stricter coverage semantics and new C2 modules.
- [x] Implement explicit imported/manual known-route input.
- [x] Implement conservative coarse EPSG:3857 request envelope.
- [x] Implement single-POST source envelope and response validation.
- [x] Implement abort/supersession owner and exact local association.
- [x] Add lazy Crime canonical-snapshot adapter without alternate ownership.
- [x] Add independent lazy chunk budget without increasing existing limits.
- [x] Repair independent review blockers: Philadelphia spatial admission,
  shared 4,096-character filter contract, first-import ownership, actual
  coverage months, canonical record/location metadata, strict dates/IDs, and
  bounded route imports.
- [x] Run focused/related tests and repaired bundle policy.
- [x] Complete independent code/data/privacy review and first-principles check.
- [x] Prepare the Lore commit and mark ready-for-integration.

## Fresh repaired evidence

- TDD RED: C1 produced `no-mapped-incidents` when new coverage semantics were
  missing; C2 produced `ERR_MODULE_NOT_FOUND` before its production modules.
- Review-repair RED included missing spatial module, omitted coverage months,
  valid full taxonomy rejection, lazy import ownership, oversized input,
  malformed IDs, and high-latitude envelope containment.
- GREEN: C1 12/12; C2 19/19; Crime async 22/22; Crime safety 9/9;
  runtime contracts 16/16; route overlays 2/2; product integrity 57/57.
- Public source probe, one POST/one statement: 46/46, not truncated; 248
  observed coverage months including `2025-06`; coverage `2006-01-01` through
  `2026-08-04`; selected-filter citywide unmapped count 286. Candidate points
  were not printed.
- Final `npm run validate`: exit 0. After a non-semantic trailing-blank-line
  cleanup, the exact committed HEAD was rebuilt and its bundle policy passed:
  Entry 889451/239906, Crime 37995/13404, Route corridor data 22908/7531,
  total dist 3448718. Existing
  Entry/Crime/CSS/total limits remain
  unchanged; only the new route chunk budget was established at 23500/7800.
- Independent architecture review: `CLEAR`; the earlier spatial coverage,
  missing-month, truncation, canonical-query, and request-owner blockers are
  closed. Independent code review: `APPROVE`, with no critical, high, or medium
  finding. The remaining low observations are a cached rejected lazy-import
  promise until reload and loss of structured boundary `sourceKind`; neither
  changes the current truthful admission result, but stage 6 should expose an
  explicit retry/unavailable path and may preserve the structured source kind.
- First-principles review rejected the earlier Turf-based 356 kB geometry path
  in favor of the smaller dependency-free conservative containment proof. No
  new package was added.
- The Crime raw budget had only 5 bytes of branch-local headroom. Combined
  I1/I2 integration therefore had to remeasure both default and CI-equivalent
  builds; any adjustment required explicit authorization and measured scope.

## Integration contract

The future S3-I1/I2 UI owns explicit user interaction and accessible map/text
presentation. It calls the lazy `requestRouteCorridor` method with an in-memory
known route and integer metre buffer. C2 captures canonical historic dates and
offense filters itself. The UI must present `coverage-unavailable`,
`source-failure`, `superseded`, and `no-mapped-incidents` distinctly and repeat
the historical/non-real-time, source, coverage, unmapped, location-precision,
reported-record/non-unique, conservative single-district admission, and
coarse-area-disclosure limitations. It must never turn this into a safety score
or route recommendation.

Integration was performed by the main-worktree owner in `b09d4a2`. The shared
Crime entry and bundle-manifest test were manually reconciled, the exact-route
privacy boundary remained unchanged, and fresh combined validation passed.
